name: Build OpenWrt Package

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4
      with:
        path: luci-theme-lumos
    
    - name: Set up environment
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses5-dev libncursesw5-dev \
          zlib1g-dev gawk git gettext libssl-dev xsltproc wget unzip python3 \
          python3-distutils python3-setuptools rsync
    
    - name: Clone OpenWrt 19.07
      uses: actions/checkout@v4
      with:
        repository: openwrt/openwrt
        ref: openwrt-19.07
        path: openwrt
        fetch-depth: 1
    
    - name: Update feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
    
    - name: Install feeds
      run: |
        cd openwrt
        ./scripts/feeds install luci-base
        ./scripts/feeds install luci-lib-ipkg
    
    - name: Copy theme to package directory
      run: |
        mkdir -p openwrt/package/luci-theme-lumos
        cp -r luci-theme-lumos/* openwrt/package/luci-theme-lumos/
    
    - name: Configure build
      run: |
        cd openwrt
        echo "CONFIG_TARGET_x86=y" >> .config
        echo "CONFIG_TARGET_x86_64=y" >> .config
        echo "CONFIG_TARGET_x86_64_Generic=y" >> .config
        echo "CONFIG_PACKAGE_luci-theme-lumos=y" >> .config
        echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config
        make defconfig
    
    - name: Build package
      run: |
        cd openwrt
        make package/luci-theme-lumos/compile V=s -j$(nproc) 2>&1 | tail -100
    
    - name: Find built package
      id: find_package
      run: |
        cd openwrt
        PACKAGE_PATH=$(find bin/packages -name "luci-theme-lumos_*.ipk" | head -1)
        echo "package_path=$PACKAGE_PATH" >> $GITHUB_OUTPUT
        echo "Found package: $PACKAGE_PATH"
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: luci-theme-lumos
        path: openwrt/${{ steps.find_package.outputs.package_path }}